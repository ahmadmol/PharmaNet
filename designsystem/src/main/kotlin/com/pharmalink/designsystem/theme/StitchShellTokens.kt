package com.pharmalink.designsystem.theme

import androidx.compose.ui.graphics.Color
import androidx.compose.ui.unit.dp

/**
 * Shared visual tokens for the main navigation shell and top-level screens
 * (Stitch “New Project”–aligned: soft canvas, elevated bar, primary accents).
 */
object StitchShellTokens {
    val canvas = ClinicalCanvas
    val navBarSurface = Color.White
    val navBarElevation = 12.dp
    val navBarTopRadius = 24.dp
    val fabRing = Color.White
}
